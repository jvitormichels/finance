.categories-box
  .show-box
    .form-box
      .form-header
        %p
          Nova categoria
      
      = form_for(:category, url: "/categories") do |f|
        .form-body
          %p
            = f.label :name, "Nome:"
            = f.text_field :name, required: true

        .form-footer
          = f.submit